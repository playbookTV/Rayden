---
title: ProgressBar
description: Linear progress indicator with basic and segmented types
---

# ProgressBar

Display linear progress indicators for loading states and task completion.

## Import

```tsx
import { ProgressBar } from "@raydenui/ui";
```

## Usage

### Basic

```tsx
<ProgressBar value={60} />
```

### Sizes

```tsx
<ProgressBar size="sm" value={40} />  {/* 4px height */}
<ProgressBar size="md" value={40} />  {/* 8px height */}
<ProgressBar size="lg" value={40} />  {/* 12px height */}
```

### Segmented Type

Display progress as 10 discrete segments instead of a continuous bar.

```tsx
<ProgressBar type="segmented" value={70} />
```

### With Label

```tsx
<ProgressBar value={75} label="Uploading files..." />
```

### With Metadata

Single metadata item:

```tsx
<ProgressBar value={60} metadata="Estimated time: 2 min" />
```

Multiple metadata items (dot-separated):

```tsx
<ProgressBar
  value={45}
  metadata={["25 MB / 50 MB", "2:30 remaining", "Fast"]}
/>
```

### Percentage Position

```tsx
// Bottom (default)
<ProgressBar value={75} percentagePosition="bottom" />

// Top
<ProgressBar value={75} percentagePosition="top" />

// Hidden
<ProgressBar value={75} showPercentage={false} />
```

### Complete Example

```tsx
<ProgressBar
  value={65}
  size="lg"
  type="basic"
  label="Downloading update..."
  metadata={["128 MB / 200 MB", "5 min left"]}
  showPercentage
  percentagePosition="top"
/>
```

### In Context

```tsx
// File upload progress
<div className="space-y-2">
  <div className="flex items-center gap-3">
    <DocumentIcon className="w-5 h-5" />
    <span className="flex-1">document.pdf</span>
  </div>
  <ProgressBar value={75} metadata="75 KB / 100 KB" />
</div>

// Task completion with segmented bar
<div className="space-y-1">
  <p className="text-sm font-medium">Profile Completion</p>
  <ProgressBar type="segmented" value={80} />
  <p className="text-xs text-grey-500">8 of 10 steps complete</p>
</div>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number` | `0` | Progress value (0-100) |
| `size` | `"sm" \| "md" \| "lg"` | `"lg"` | Bar height: sm (4px), md (8px), lg (12px) |
| `type` | `"basic" \| "segmented"` | `"basic"` | Continuous or segmented bar |
| `label` | `string` | — | Label text above the bar |
| `metadata` | `string \| string[]` | — | Metadata text(s) below the bar |
| `showPercentage` | `boolean` | `true` | Show percentage text |
| `percentagePosition` | `"top" \| "bottom"` | `"bottom"` | Position of percentage text |
| `className` | `string` | — | Additional CSS classes |

## Accessibility

- Uses `role="progressbar"` with ARIA attributes
- `aria-valuenow`, `aria-valuemin`, `aria-valuemax` set correctly
- Works with screen readers
